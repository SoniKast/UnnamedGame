/// @description Camera lag.
 // This prevents the camera from moving. This happens when the character performs certain actions, such as the spindash.

    if(cam_lag > 0)
    {
       cam_lag -= 1;
    }